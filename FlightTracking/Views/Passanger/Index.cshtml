@model IEnumerable<FlightTracking.Models.Passanger>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "AddPassanger")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.currentStage.ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nationality)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.currentStage.ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nationality)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CurrentStageId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CurrentStageId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CurrentStageId })
        </td>
    </tr>
}

</table>
