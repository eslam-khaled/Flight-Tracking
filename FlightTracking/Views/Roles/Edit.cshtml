@model Microsoft.AspNet.Identity.EntityFramework.IdentityRole
@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_LayoutInternal.cshtml";
}

<h2>Edit Role</h2>

@Html.ActionLink("Create New Role", "Create", null, new { @class = "btn btn-primary" }) | @Html.ActionLink("Manage User Role", "ManageUserRoles", null, new { @class = "btn btn-primary" })
<hr />
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
    @Html.HiddenFor(model => model.Id)
    <div class="">
        <lable>Role name : </lable>
    <div>
        @Html.TextBoxFor(model => model.Name ,htmlAttributes:new { @class="form-control",placeholder="Role Name"})
    </div>
    </div>
    <div>
    <input type="submit" class="btn btn-success" value="Save" />
    </div>
}

